# 发布功能使用指南

## 功能概述

小红书自动发布系统现已支持**一键发布到小红书**功能！使用 Playwright 浏览器自动化技术，模拟真实用户操作，实现自动发布图文内容。

## 使用前准备

### 1. 安装浏览器驱动

首次使用前，系统会自动安装 Playwright 浏览器驱动。也可以手动安装：

```bash
python -m playwright install chromium
```

### 2. 启动系统

```bash
./start.sh
```

系统会自动：
- 创建虚拟环境（首次）
- 安装依赖包（首次）
- 安装浏览器驱动（首次）
- 启动 Web 服务

### 3. 访问界面

打开浏览器访问：http://localhost:8000

## 发布流程

### 步骤1：登录小红书

1. 在首页顶部发布状态栏，查看登录状态
2. 如果显示"未登录"，点击"登录小红书"按钮
3. 系统会打开浏览器窗口
4. 在浏览器中使用**小红书APP扫码登录**
5. 登录成功后，状态栏会显示"已登录"

**注意：**
- 登录状态会持久化保存，下次启动无需重新登录
- 首次使用建议设置 `headless: false`（默认），便于扫码登录
- 登录成功后，可以在配置中修改为 `headless: true`，实现后台自动发布

### 步骤2：生成并审核内容

1. 点击"生成内容"
2. 输入主题、选择风格、AI提供商
3. 点击"生成内容"按钮
4. 查看生成的内容，可以编辑修改
5. 确认无误后，点击"确认保存"
6. 在草稿详情页，点击"批准"按钮

### 步骤3：发布到小红书

1. 在草稿详情页（状态为"已审核"），点击"发布到小红书"按钮
2. 系统会自动打开浏览器（如果设置了 headless: false）
3. 自动执行发布流程：
   - 访问小红书创作者中心
   - 上传图片
   - 填写标题和正文
   - 添加话题标签
   - 点击发布
4. 等待发布完成（约10-30秒）
5. 发布成功后：
   - 草稿状态变为"已发布"
   - 显示发布时间
   - 显示发布链接（可点击查看）

## 发布限制

为避免被判定为营销号，系统设置了发布限制：

- **每日发布上限**：3条（可在配置文件中修改）
- **发布间隔**：5分钟（可在配置文件中修改）

发布状态栏会实时显示：
- 今日已发布数量
- 剩余发布配额
- 下次可发布时间

## 发布失败处理

如果发布失败，系统会：
1. 将草稿状态标记为"发布失败"
2. 显示具体的错误信息
3. 可以查看错误原因并修改后重新发布

常见失败原因：
- 未登录或登录失效
- 标题超过20字
- 图片数量超过9张
- 网络异常
- 小红书平台限制

## 配置说明

编辑 `config/config.yaml` 文件可以自定义发布行为：

```yaml
publish:
  daily_limit: 3          # 每日发布上限
  interval: 300           # 发布间隔（秒）

  browser:
    headless: false       # 是否使用无头模式
    storage_path: ./data/browser_state
    timeout: 30000        # 操作超时（毫秒）

  retry:
    max_retries: 3        # 失败重试次数
    base_delay: 5         # 重试延迟（秒）

  login:
    max_wait_time: 300    # 等待登录超时（秒）
```

### headless 模式说明

- **headless: false**（推荐首次使用）
  - 会显示浏览器窗口
  - 便于观察发布过程
  - 便于扫码登录
  - 出错时容易调试

- **headless: true**（推荐登录成功后）
  - 不显示浏览器窗口
  - 后台自动发布
  - 更快速、更稳定
  - 适合定时自动发布

## 注意事项

### ⚠️ 重要提醒

1. **遵守平台规则**
   - 不要过度发布（建议每天不超过3-5条）
   - 内容要真实、有价值
   - 避免生成违规内容

2. **账号安全**
   - 使用真实的小红书账号
   - 不要频繁更换设备登录
   - 建议开启二次验证

3. **内容质量**
   - AI生成的内容要人工审核
   - 添加个人见解和修改
   - 确保图片质量

4. **发布频率**
   - 严格遵守系统发布限制
   - 建议在不同时间段发布
   - 避免短时间内连续发布

## 故障排除

### Q: 登录失败怎么办？
A:
- 检查网络连接
- 确认使用小红书APP扫码
- 尝试清除浏览器状态重新登录
- 检查是否开启二次验证

### Q: 发布超时怎么办？
A:
- 检查网络速度
- 增加配置中的 timeout 值
- 检查图片文件大小
- 尝试减少图片数量

### Q: 如何查看发布日志？
A:
- 查看终端输出
- 查看草稿详情页的错误信息
- 查看浏览器控制台（headless: false）

### Q: 如何批量发布？
A:
目前系统支持单个发布。批量发布功能正在开发中。

## 技术原理

发布功能基于 **Playwright** 浏览器自动化框架：

1. **状态持久化**
   - 使用 `launch_persistent_context`
   - 自动保存 cookies 和登录状态
   - 无需每次重新登录

2. **反爬虫策略**
   - 隐藏自动化特征
   - 修改 navigator 属性
   - 模拟真实用户行为

3. **智能等待**
   - 自动等待页面加载
   - 等待元素出现
   - 处理异步操作

4. **错误处理**
   - 异常捕获和重试
   - 详细的错误信息
   - 状态回滚

## 反馈与建议

如有问题或建议，欢迎反馈：
- 查看项目 README.md
- 检查更新日志
- 提交 Issue

---

祝你使用愉快！🎉