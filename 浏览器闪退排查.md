# 🔍 浏览器闪退问题排查步骤

## 第一步：独立诊断测试

运行诊断脚本，测试浏览器是否能正常保持打开：

```bash
cd 小红书自动发布
source venv/bin/activate
python diagnose_browser.py
```

**预期结果：**
- 浏览器窗口打开
- 访问百度首页
- 窗口**保持打开**，不会自动关闭
- 终端显示 "✓✓✓ 诊断成功！浏览器窗口应该保持打开"

**如果窗口仍然关闭：**
说明Playwright本身有问题，可能需要：
1. 重新安装Playwright
2. 检查系统权限
3. 尝试不同的浏览器驱动

## 第二步：检查Web应用

如果诊断脚本通过，说明浏览器管理器正常，问题在Web应用。

### 2.1 重启服务

```bash
# 停止服务（Ctrl+C）
# 重新启动
cd 小红书自动发布
source venv/bin/activate
python main.py web
```

### 2.2 查看终端日志

启动后应该看到：
```
应用启动中...
✓ 发布器实例已创建
```

### 2.3 触发登录

1. 访问 http://localhost:8000
2. 点击"登录小红书"
3. **立即查看终端输出**

**关键日志：**
```
============================================================
正在初始化浏览器...
============================================================
✓ 浏览器已初始化并打开
✓ 浏览器窗口将保持打开状态
============================================================
```

### 2.4 观察浏览器窗口

- 如果看到窗口打开后立即关闭 → 问题仍然存在
- 如果窗口保持打开 → 问题已解决

## 第三步：检查配置

确保配置正确：

```bash
cat config/config.yaml | grep -A 10 "browser:"
```

**应该看到：**
```yaml
browser:
  headless: false  # 必须是 false
  storage_path: ./data/browser_state
  timeout: 30000
```

## 第四步：环境检查

### 4.1 检查Playwright版本

```bash
source venv/bin/activate
python -c "import playwright; print(playwright.__version__)"
```

### 4.2 重新安装浏览器驱动

```bash
source venv/bin/activate
python -m playwright install chromium
```

### 4.3 检查系统权限

```bash
# 检查数据目录权限
ls -la data/
# 如果没有 browser_state 目录，创建它
mkdir -p data/browser_state
```

## 第五步：手动测试Playwright

创建一个最简单的测试：

```bash
source venv/bin/activate
python3 << 'EOF'
import asyncio
from playwright.async_api import async_playwright

async def test():
    print("启动Playwright...")
    playwright = await async_playwright().start()

    print("创建浏览器上下文...")
    context = await playwright.chromium.launch_persistent_context(
        user_data_dir='./test_browser',
        headless=False
    )

    print("创建页面...")
    page = await context.new_page()

    print("访问页面...")
    await page.goto('https://www.baidu.com')

    print("✓ 成功！浏览器窗口将保持打开10秒...")
    await asyncio.sleep(10)

    print("窗口关闭测试完成")

    # 不关闭，让系统自动清理
    # await context.close()

asyncio.run(test())
EOF
```

**预期：** 浏览器窗口打开，显示百度，保持10秒后脚本结束，窗口关闭。

**如果窗口立即关闭：** Playwright本身有问题。

## 第六步：查看详细错误

如果所有测试都失败，获取详细错误信息：

```bash
source venv/bin/activate
python main.py web 2>&1 | tee debug.log
```

然后在另一个终端：
```bash
# 触发登录
curl -X POST http://localhost:8000/api/publish/login
```

查看 `debug.log` 文件中的错误信息。

## 常见问题和解决方案

### 问题1：Playwright浏览器驱动未安装

**症状：** `ModuleNotFoundError: No module named 'playwright'`

**解决：**
```bash
source venv/bin/activate
pip install playwright
python -m playwright install chromium
```

### 问题2：权限问题

**症状：** 浏览器无法创建用户数据目录

**解决：**
```bash
mkdir -p data/browser_state
chmod 755 data/browser_state
```

### 问题3：端口冲突

**症状：** 服务无法启动

**解决：**
```bash
# 查找占用8000端口的进程
lsof -i:8000
# 杀掉进程
kill -9 <PID>
```

### 问题4：Python版本问题

**症状：** 语法错误或导入错误

**解决：** 确保Python版本 >= 3.8

```bash
python3 --version
```

## 终极解决方案

如果以上都无法解决，尝试完全重新安装：

```bash
cd 小红书自动发布

# 1. 删除虚拟环境
rm -rf venv

# 2. 重新创建
python3 -m venv venv
source venv/bin/activate

# 3. 安装依赖
pip install -r requirements.txt

# 4. 安装Playwright
pip install playwright
python -m playwright install chromium

# 5. 启动服务
python main.py web
```

## 需要提供的信息

如果问题仍然存在，请提供：

1. **诊断脚本输出**
   ```bash
   python diagnose_browser.py > diagnose_output.txt 2>&1
   cat diagnose_output.txt
   ```

2. **系统信息**
   ```bash
   sw_vers  # macOS
   python3 --version
   ```

3. **终端完整日志**
   从启动服务到触发登录的完整输出

4. **错误截图**
   如果有任何错误弹窗

---

更新时间：2026-09-17
诊断脚本版本：v1.0