# 问题修复说明

## 🐛 已修复的问题

### 1. 配置保存问题
**问题**: 点击保存配置后，配置变空
**原因**: 后端使用了`if request.bailian_api_key:`判断，空字符串不会保存
**修复**: 改为`if request.bailian_api_key is not None:`，允许保存空值

### 2. 模型默认值问题
**问题**: bailian_generator默认模型是'qwen-plus'
**原因**: 旧版本的模型ID
**修复**: 更新为'qwen3.7-plus'

### 3. 生成器支持问题
**问题**: 提示"不支持的AI提供商:bailian"
**原因**: bailian_generator.py已存在且正确
**状态**: 应该可以正常工作

---

## ✅ 验证修复

### 方法一：测试脚本

运行测试脚本验证配置：

```bash
python test_bailian.py
```

这个脚本会：
1. 检查配置是否正确
2. 测试生成器是否能创建
3. 尝试生成一个简单的内容

### 方法二：Web界面测试

1. 重启Web服务：
   ```bash
   # 停止当前服务 (Ctrl+C)
   ./start.sh
   ```

2. 访问设置页面：http://localhost:8000 → 设置

3. 重新配置：
   - 输入百炼API Key
   - 选择模型
   - 点击保存

4. 测试生成：
   - 点击「生成内容」
   - 输入主题
   - 点击「生成内容」按钮

---

## 🔍 如何检查配置

### 查看配置文件

```bash
cat config/config.yaml
```

应该看到类似这样的内容：
```yaml
ai:
  bailian:
    api_key: sk-xxxxx...
    base_url: https://coding.dashscope.aliyuncs.com/apps/anthropic
    max_tokens: 2000
    model: qwen3.7-plus
```

### 查看日志

如果生成失败，查看终端输出的错误信息。

---

## 🐛 如果还有问题

### 问题1: 配置保存后仍然为空

**检查方法**:
```bash
cat config/config.yaml
```

如果确实为空，手动编辑：
```bash
nano config/config.yaml
```

添加以下内容：
```yaml
ai:
  bailian:
    api_key: "你的API密钥"
    base_url: "https://coding.dashscope.aliyuncs.com/apps/anthropic"
    max_tokens: 2000
    model: "qwen3.7-plus"
```

### 问题2: 生成失败

**检查API密钥是否正确**:
- 确保密钥以 `sk-` 开头
- 确保密钥没有多余空格
- 确保密钥在百炼平台上是有效的

**检查模型ID**:
- 使用 `qwen3.7-plus` 或 `qwen3-max-2026-01-23`
- 不要使用 `qwen-plus` 或其他旧版本ID

**检查网络**:
- 确保可以访问 `https://dashscope.aliyuncs.com`
- 如果在国内，应该没有问题

---

## 📞 技术支持

如果以上方法都无法解决问题，请：

1. 运行测试脚本：`python test_bailian.py`
2. 查看错误信息
3. 提供完整的错误日志

---

**更新时间**: 2026-09-16